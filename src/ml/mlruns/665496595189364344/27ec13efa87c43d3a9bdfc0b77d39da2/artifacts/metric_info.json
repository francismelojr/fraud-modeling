{
  "accuracy_score-2_X_test": "accuracy_score(y_true=y_test, y_pred=y_test_pred)",
  "accuracy_score-3_X_test": "accuracy_score(y_true=y_test, y_pred=y_test_pred)",
  "accuracy_score-4_X_train": "accuracy_score(y_true=y_train, y_pred=y_train_pred)",
  "accuracy_score-5_X_train": "accuracy_score(y_true=y_train, y_pred=y_train_pred)",
  "accuracy_score_X_train": "accuracy_score(y_true=y_train, y_pred=y_train_pred)",
  "f1_score-2_X_test": "f1_score(y_true=y_test, y_pred=y_test_pred)",
  "f1_score-3_X_test": "f1_score(y_true=y_test, y_pred=y_test_pred)",
  "f1_score-4_X_train": "f1_score(y_true=y_train, y_pred=y_train_pred)",
  "f1_score-5_X_train": "f1_score(y_true=y_train, y_pred=y_train_pred)",
  "f1_score_X_train": "f1_score(y_true=y_train, y_pred=y_train_pred)",
  "precision_score-2_X_test": "precision_score(y_true=y_test, y_pred=y_test_pred)",
  "precision_score-3_X_test": "precision_score(y_true=y_test, y_pred=y_test_pred)",
  "precision_score-4_X_train": "precision_score(y_true=y_train, y_pred=y_train_pred)",
  "precision_score-5_X_train": "precision_score(y_true=y_train, y_pred=y_train_pred)",
  "precision_score_X_train": "precision_score(y_true=y_train, y_pred=y_train_pred)",
  "recall_score-2_X_test": "recall_score(y_true=y_test, y_pred=y_test_pred)",
  "recall_score-3_X_test": "recall_score(y_true=y_test, y_pred=y_test_pred)",
  "recall_score-4_X_train": "recall_score(y_true=y_train, y_pred=y_train_pred)",
  "recall_score-5_X_train": "recall_score(y_true=y_train, y_pred=y_train_pred)",
  "recall_score_X_train": "recall_score(y_true=y_train, y_pred=y_train_pred)",
  "roc_auc_score-10_X_train": "roc_auc_score(y_true=y_train, y_score=y_train_pred)",
  "roc_auc_score-11_X_train": "roc_auc_score(y_true=y_train, y_score=y_train_pred)",
  "roc_auc_score-12_X_train": "roc_auc_score(y_true=y_train, y_score=y_train_pred)",
  "roc_auc_score-2_X_train": "roc_auc_score(y_true=y_train, y_score=y_train_pred)",
  "roc_auc_score-3_X_train": "roc_auc_score(y_true=y_train, y_score=y_train_pred)",
  "roc_auc_score-4_X_train": "roc_auc_score(y_true=y_train, y_score=y_train_pred)",
  "roc_auc_score-5_X_train": "roc_auc_score(y_true=y_train, y_score=y_train_pred)",
  "roc_auc_score-6_X_train": "roc_auc_score(y_true=y_train, y_score=y_train_pred)",
  "roc_auc_score-7_X_train": "roc_auc_score(y_true=y_train, y_score=y_train_pred)",
  "roc_auc_score-8_X_train": "roc_auc_score(y_true=y_train, y_score=y_train_pred)",
  "roc_auc_score-9_X_train": "roc_auc_score(y_true=y_train, y_score=y_train_pred)",
  "roc_auc_score_X_train": "roc_auc_score(y_true=y_train, y_score=y_train_pred)"
}